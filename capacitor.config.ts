import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ygramul.bonsaiGarden',
  appName: 'My Bonsai Garden',
  webDir: 'dist',
};

export default config;
